sub createOverviewMap {
    # get StartpointCoordinates + date + name

    # partitionate stratpoints in groups for usefull map zoom

    # download + plot maps

    # add starticons with refs ('\label{',$Activity_date,'-',$Goal,'}') to tour


}
1;
